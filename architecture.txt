rag-project/
│
├── backend/
│   ├── venv/                          # python -m venv venv (gitignored)
│   ├── .env                           # API keys (gitignored)
│   ├── .env.example
│   ├── requirements.txt
│   ├── main.py                        # FastAPI app entrypoint, route registration
│   │
│   ├── api/
│   │   ├── routes_upload.py           # POST /upload  → ingest documents
│   │   ├── routes_query.py            # POST /query   → run RAG pipeline
│   │   ├── routes_config.py           # GET  /methods → list available retrieval methods
│   │
│   ├── core/
│   │   ├── document_loader.py         # PDF/txt/docx parsing → raw text
│   │   ├── chunking.py                # fixed-size, recursive, semantic chunking strategies
│   │   ├── embeddings.py              # wraps embedding API/model calls
│   │   ├── vector_store.py            # numpy brute-force store + FAISS/Chroma variants
│   │   ├── retrieval/
│   │   │   ├── semantic_search.py
│   │   │   ├── keyword_search.py      # BM25
│   │   │   ├── hybrid_search.py       # semantic + keyword fusion (RRF etc.)
│   │   │   ├── reranker.py            # cross-encoder reranking step
│   │   ├── generation.py              # prompt assembly + LLM call
│   │   ├── multimodal.py              # image/table extraction + multimodal embedding path
│   │   ├── evaluation.py              # retrieval metrics (precision@k, MRR, etc.), latency/token tracking
│   │
│   ├── models/
│   │   ├── schemas.py                 # Pydantic request/response models
│   │
│   ├── storage/
│   │   ├── uploaded_docs/             # raw uploaded files (gitignored)
│   │   ├── vector_indices/            # persisted embeddings/index (gitignored)
│
├── frontend/
│   ├── node_modules/                  # gitignored
│   ├── package.json
│   ├── src/
│   │   ├── App.jsx
│   │   ├── components/
│   │   │   ├── UploadPanel.jsx
│   │   │   ├── QueryPanel.jsx
│   │   │   ├── MethodSelector.jsx     # dropdown: semantic/hybrid/multimodal/etc.
│   │   │   ├── ResultsPanel.jsx       # LLM response display
│   │   │   ├── MetricsPanel.jsx       # tokens, latency, per-step timing
│   │   │   ├── RetrievedChunksPanel.jsx
│   │   │   ├── EvalMetricsPanel.jsx
│   │   ├── api/
│   │   │   ├── client.js              # fetch wrappers to FastAPI endpoints
│
├── .gitignore
└── README.md